## 0.0.1

- Initial alpha release
- Added `EncresaSystem.connect()` for AURA Protocol initialization
- Added `EncresaSystem.authenticate()` placeholder
- Added `AuthResult` class for authentication responses
